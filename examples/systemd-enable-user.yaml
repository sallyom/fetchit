targetConfigs:
- url: https://localhost:10080/sallyom/fetchit-local
  systemd:
  - name: httpd-user
    targetPath: examples/systemd
    root: false
    enable: true
    schedule: "*/1 * * * *"
  branch: main

