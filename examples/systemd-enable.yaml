targetConfigs:
- url: http://localhost:10080/sallyom/fetchit-local
  systemd:
  - name: httpd-root
    targetPath: examples/systemd
    root: true
    enable: true
    schedule: "*/1 * * * *"
  branch: main
