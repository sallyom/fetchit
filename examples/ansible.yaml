targetConfigs:
- url: http://localhost:10080/sallyom/fetchit-local
  ansible:
    - name: ansible
      targetPath: examples/ansible
      sshDirectory: /root/.ssh
      schedule: "*/1 * * * *"
  branch: main
