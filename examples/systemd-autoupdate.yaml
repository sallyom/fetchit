podmanAutoUpdate:
  root: true
targetConfigs:
- url: http://localhost:10080/sallyom/fetchit-local
  systemd:
  - name: sysd-ex
    targetPath: examples/systemd
    root: true
    enable: true
    schedule: "*/5 * * * *"
  branch: main
