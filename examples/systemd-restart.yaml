targetConfigs:
- url: http://localhost:10080/sallyom/fetchit-local
  systemd:
  - name: sysd-ex
    targetPath: examples/systemd
    root: true
    enable: true
    restart: true
    schedule: "*/1 * * * *"
  branch: main
